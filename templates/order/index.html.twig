{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">

		<h1 class="text-decoration-underline my-5">Ma commande</h1>

		<div class="row w-100">
			<div class="col-md-3"></div>
			<div class="col-md-6">
				{{ form_start(deliveryForm) }}
				{{ form_row(deliveryForm.addresses) }}
                <a href="{{ path('app_account_address_form') }}" class="text-decoration-none pt-1 pb-4 small">Ajouter une adresse de livraison.</a>
                <hr>
				{{ form_row(deliveryForm.carriers) }}

				{{ form_end(deliveryForm) }}
				</div>
				<div class="col-md-3"></div>


			</div>
		</div>


	{% endblock %}
