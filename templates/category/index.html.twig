{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		
		<h3 class="text-decoration-underline mt-5">{{ category.name }}</h3>
		<h5 class="mb-5">Cette catégorie contient
			{{ category.products|length  }}
			produit{{ category.products|length != 1 ? 's' : '' }}.</h5>

		{# {{ dump(category.products) }} #}

		<div class="row row-gap-4">
			{% for product in category.products %}
				{# {{ dump(product) }} #}
				{{ include('category/card.html.twig', { 'product': product }) }}
			{% endfor %}
		</div>


	</div>
</div>{% endblock %}
