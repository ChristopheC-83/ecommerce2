{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		
		{# {{ dump(categories) }} #}
		{# {{ category.products|length  }} #}

		<h3 class="text-decoration-underline mt-5">Catalogue complet :</h3>
		<h5 class="mb-5">Notre catalogue contient {{ products|length}} produits.</h5>
		<div class="row row-gap-4">
			{% for category in categories %}
				{% for product in category.products %}
					{{ include('category/card.html.twig', { 'product': product }) }}
				{% endfor %}
			{% endfor %}
		</div>

	</div>
{% endblock %}
